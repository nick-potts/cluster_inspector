#!/bin/sh

# Railway clustering setup
# Requires: RAILWAY_PRIVATE_DOMAIN set by Railway

if [ -n "$RAILWAY_PRIVATE_DOMAIN" ]; then
  # Use short names for distributed Erlang (works with raw IPv4 addresses)
  export RELEASE_DISTRIBUTION=sname
  export RELEASE_NODE="cluster_monitor"
fi
