#!/bin/sh

# Railway clustering setup
# Requires: RAILWAY_PRIVATE_DOMAIN set by Railway

if [ -n "$RAILWAY_PRIVATE_DOMAIN" ]; then
  # Use long names for distributed Erlang with container hostname
  export RELEASE_DISTRIBUTION=name
  HOSTNAME=$(hostname -f)
  export RELEASE_NODE="cluster_monitor@${HOSTNAME}"
fi
